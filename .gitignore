# --- Build outputs (generated by build.bat) ---
target/
*.lst
*.prg
*.bin
*.d81

# Keep source/asset binaries that are NOT build outputs
!basic/cursor.bin

# --- Build tools (committed on purpose) ---
# Ignore stray .exe files but keep the toolchain we ship in-repo
*.exe
!64tass.exe
!c1541.exe
!petcat.exe

# --- Scratch / editor / OS junk ---
tmp.txt
*.tmp
*.bak
*.swp
Thumbs.db
desktop.ini
.DS_Store

# --- Local tooling ---
.claude/
